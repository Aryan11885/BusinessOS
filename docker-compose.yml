version: "3.9"

services:

  backend:
    build: ./businessos-backend
    container_name: businessos-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./businessos-frontend
    container_name: businessos-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend